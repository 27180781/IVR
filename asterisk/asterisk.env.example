# Values substituted into asterisk/*.conf by scripts/deploy-asterisk.sh.
# Copy to asterisk/asterisk.env and fill in. Never commit the real file.

PUBLIC_IP=203.0.113.10
IVR_LANGUAGE=he
ARI_PASSWORD=change-me-to-a-long-random-string
# Only needed for outbound calls. Your Twilio Termination SIP Domain:
TWILIO_TERMINATION_DOMAIN=your-trunk.pstn.twilio.com
